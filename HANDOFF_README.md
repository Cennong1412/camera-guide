# 카메라 설정 추천 앱 — 클로드 코드 핸드오프 가이드

## 이 파일들이 무엇인가

- `camera_settings_spec.json`: 촬영 조건(대상/환경/목적/의도/안정화/기법/장비)을 넣으면
  카메라 설정 14개 항목이 계산되어 나오는 **규칙엔진 스펙**입니다.
  39,168개(또는 64,584개) 조합을 전부 저장해두는 대신, 훨씬 작은 기본값 표
  (대상×환경×목적 = 2,904개 조합에 필요한 기본값)에 나머지 축(의도/안정화/기법/장비)을
  "보정 규칙"으로 얹어 실행 시점에 계산하는 구조입니다.

## 왜 이 구조로 했는지

원래 조합 수(최대 64,584개, 필터링 후 39,168개)를 전부 정적 테이블로 만들면
14개 설정 항목 × 2개 장비유형까지 곱해 수십만~백만 단위의 값을 채워야 해서
- 손으로 채우기엔 비현실적으로 많고
- AI로 자동 채워도 항목 간 정합성(예: 천체 사진인데 어떤 줄은 ISO 800, 다른 줄은 ISO 6400)이
  깨지기 쉽고
- 나중에 규칙 하나만 바꿔도 수만 줄을 다시 손봐야 합니다.

그래서 "대상 대분류(10종)별 기본값 + 환경/목적 보정 + 의도/안정화/기법/장비 보정"
구조로 설계했습니다. 이러면 실제로 사람이 검토·수정해야 할 데이터량이
2,904개가 아니라 **기본값 10줄 + 보정 규칙 몇십 줄** 수준으로 줄어듭니다.

## 클로드 코드에서 구현할 때 순서

`camera_settings_spec.json`의 `resolution_order`를 그대로 따르면 됩니다:

1. UI에서 사용자가 대상/환경/목적/의도/안정화/기법/장비를 선택
2. `validity_rules`(R1~R7)로 대상×환경×기법 조합이 유효한지 먼저 검사
   - 유효하지 않은 조합은 애초에 UI에서 선택 불가하게 만드는 걸 추천합니다
     (예: 대상=천체 선택 시 환경 드롭다운에서 "한낮" 같은 옵션은 자동으로 숨김)
3. `base_rules_by_대상_대분류`에서 대상의 기본 설정값 조회
4. `environment_modifiers_by_대분류` → `purpose_modifiers` → `delta_rules.의도` →
   `delta_rules.안정화` → `delta_rules.기법` 순서로 값 보정
5. 마지막에 `delta_rules.장비`를 적용해 스마트폰용/미러리스용 최종 14개 필드값 확정
   - 스마트폰은 `settings_fields`의 `smartphone` 설명대로 일부 항목이
     "촬영모드 프리셋"이나 "렌즈 선택"처럼 단순화된 값으로 대체됩니다

## 클로드 코드에 던질 첫 프롬프트 예시

```
camera_settings_spec.json 파일을 참고해서 다음 기능을 만들어줘:
1. spec의 axes를 기반으로 대상/환경/목적/의도/안정화/기법/장비를 선택하는 UI
2. validity_rules를 이용해 선택 불가능한 조합은 미리 걸러주는 로직
3. resolution_order를 따라 base_rules → environment_modifiers → purpose_modifiers
   → delta_rules(의도/안정화/기법/장비) 순으로 최종 14개 카메라 설정값을 계산하는 함수
4. 계산 결과를 보여주는 결과 화면
```

이 스펙 파일 자체를 그대로 앱 코드(또는 서버)에 데이터로 포함시켜서
클로드 코드가 파싱해 쓰도록 넘기면 됩니다.

## 남은 작업 (검토/보완이 필요한 부분)

- `base_rules_by_대상_대분류`는 대상 **대분류(10종)** 기준 대표값입니다.
  같은 "인물"이라도 웨딩과 증명사진은 실제로는 세부값이 달라질 수 있는데,
  이번 버전에서는 소분류(26종)별 세분화는 생략했습니다. 필요하면 이후에
  소분류 단위 override를 추가하는 식으로 확장 가능합니다.
- 값들은 일반적인 사진 촬영 가이드 수준의 범위값이며, 실제 사용하시는
  카메라 기종/렌즈에 따라 조정이 필요할 수 있습니다.
- `validity_rules`(R1~R7)는 제가 판단한 논리적 배제 기준입니다.
  실제 써보시면서 과하거나 부족한 규칙이 보이면 조정해 나가면 됩니다.
